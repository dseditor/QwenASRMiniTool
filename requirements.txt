# Qwen3 ASR MiniTool - 執行環境依賴
# 安裝：pip install -r requirements.txt

# GUI
customtkinter>=5.2.0
# WebView 版介面：原生 WebView2 視窗（系統 runtime，不打包 Chromium）。
# 僅用 pywebview「載入本機網址」開窗，不使用其 js_api（避開 pythonnet
# 序列化的遞迴 bug）；前端與本機 HTTP server 走 HTTP/SSE 溝通。
pywebview>=6.0

# 推理引擎
openvino>=2024.0.0
onnxruntime>=1.17.0

# 音訊處理
numpy>=1.24.0
librosa>=0.10.0
sounddevice>=0.4.6

# 中文轉換（簡轉繁）
opencc-python-reimplemented>=0.1.7

# ──────────────────────────────────────────────────────
# 以下僅開發時需要（用於重新產生 prompt_template.json）
# pip install qwen-asr transformers
# python generate_prompt_template.py
